<h1>Foods Available</h1>
<div>
    <input type="button" class="btn btn-success" value="Add Food" onclick="location.href='@Url.Action("addFood", "Food")'" />
    @foreach (var f in Model)
    {
        <div>
            <h3>@f.Food.Name - @f.Units @Html.ActionLink("Delete", "delete", new { id = f.PantryID }) | @Html.ActionLink("Update", "update", new {id = f.PantryID })</h3>
        </div>
    }
</div>